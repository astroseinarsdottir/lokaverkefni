<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:weightSum="1"
    android:layout_marginBottom="2dp"
    android:paddingTop="2dp"
    android:paddingRight="2dp"
    android:paddingEnd="2dp">

    <ImageView
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:id="@+id/imageView_companyLogo"
        app:srcCompat="@mipmap/ic_launcher"/>

    <TextView
        android:id="@+id/textView_title"
        android:layout_toRightOf="@+id/imageView_companyLogo"
        android:layout_toEndOf="@+id/imageView_companyLogo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp"
        android:layout_marginLeft="20dp"
        android:textSize="22sp"
        android:textColor="@color/toolbarColor"/>

    <ImageView
        android:id="@+id/imageView_discountProvider"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:gravity="end"
        app:srcCompat="@mipmap/ic_launcher"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <ImageView
        android:id="@+id/imageView_condition"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginTop="18dp"
        android:gravity="end"
        app:srcCompat="@mipmap/ic_launcher"
        android:layout_below="@+id/imageView_discountProvider"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

</RelativeLayout>